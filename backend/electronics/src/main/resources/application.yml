spring:
  application:
    name: electronics

  # 1. Database Connection
  datasource:
    url: jdbc:postgresql://localhost:5432/electronics_management
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

  # 2. Hibernate
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  # 3. Upload Image
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

# 3. JWT
electronics:
  app:
    jwtSecret: DayLaMotDoanMaBiMatCuaDuAnElectronicsDaiHon32KyTuDeBaoMat
    jwtExpirationMs: 86400000

# 4. Payment Integration
payment:
  vnpay:
    tmn-code: "YOUR_TMN_CODE" # Lấy trên trang Sandbox VNPay
    secret-key: "YOUR_SECRET_KEY" # Lấy trên trang Sandbox VNPay
    pay-url: "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"
    refund-url: "https://sandbox.vnpayment.vn/merchant_webapi/api/transaction"
    return-url: "http://localhost:8080/api/payments/vnpay-return" # Đường dẫn gọi về Frontend/Backend của sếp sau khi trả tiền
  momo:
    partner-code: "MOMO_PARTNER_CODE"
    access-key: "MOMO_ACCESS_KEY"
    secret-key: "MOMO_SECRET_KEY"
    endpoint: "https://test-payment.momo.vn/v2/gateway/api/create"
    refund-url: "https://test-payment.momo.vn/v2/gateway/api/refund"
    return-url: "http://localhost:8080/api/payments/momo-return"
    notify-url: "http://localhost:8080/api/webhook/momo-ipn" # Đường dẫn Momo bắn IPN về

# Cấu hình Cloudinary
cloudinary:
  cloud-name: ddftruti7
  api-key: 137853177264324
  api-secret: _S5YYj3wj-QzsBc9KEq3z5Q10pM